input {
  elastic_agent {
    port => 5044
    ssl => true
    ssl_certificate => "/usr/share/logstash/ssl/instance.crt"
    ssl_key => "/usr/share/logstash/ssl/instance.pkcs8.key"
  }
}

output {
  stdout {
    codec => rubydebug
  }
}